name: Release

on:
  push:
    branches: [master]

env:
  REGISTRY: ghcr.io

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Compute lowercase image prefix
        id: image
        run: echo "prefix=$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_OUTPUT"

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        run: |
          echo "sha_short=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Build and push frontend image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.frontend
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ steps.image.outputs.prefix }}/frontend:latest
            ${{ env.REGISTRY }}/${{ steps.image.outputs.prefix }}/frontend:${{ steps.meta.outputs.sha_short }}

      - name: Build and push nakama image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: nakama/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ steps.image.outputs.prefix }}/nakama:latest
            ${{ env.REGISTRY }}/${{ steps.image.outputs.prefix }}/nakama:${{ steps.meta.outputs.sha_short }}
